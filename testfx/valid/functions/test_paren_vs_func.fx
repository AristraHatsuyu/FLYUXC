// 测试括号表达式 vs 函数定义的区分

// 正常的括号表达式（变量赋值）
x := (1 + 2) * 3

// 括号表达式（嵌套）
y := ((5 - 2) * (3 + 1))

// 无类型函数定义
f := (a, b) {
    R> a + b
}

// 无参数函数
g := () {
    R> 100
}

// main 函数
main := () {
    result := f(x, y)
    R> result + g()
}
