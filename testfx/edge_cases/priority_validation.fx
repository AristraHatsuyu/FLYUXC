/* 验证运算符优先级的正确性 */

// 如果 && 优先级低于 |，那么：
// a | b && c & d 应该等同于 a | ((b && c) & d)？

// 让我们测试实际的执行顺序
test1 := a | b && c

// 按当前优先级:
// & (40) > | (35) > && (30)
// 所以应该是: a | (b && c)

// 但这是错的！标准C语言中:
// && (40) > | (50)  ← 我搞反了！

// 标准应该是:
// & > && > |
// 所以 a | b && c 应该是 (a | b) && c  ← 错！
// 标准C是: a | (b && c)  ← 对！

// 让我创建明确的测试
explicit1 := (a | b) && c    // 期望保留括号
explicit2 := a | (b && c)    // 期望可以去括号变成 a|b&&c
