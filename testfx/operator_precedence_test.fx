/* 测试运算符优先级和去括号功能 */

// 测试1: 位运算符优先级
// & > ^ > | 的优先级顺序
test1 := ((a | b) ^ c) & d    // 外层括号应该被去掉
test2 := a | (b ^ c) & d      // 内层括号应该被保留（确保正确优先级）

// 测试2: 幂运算符优先级
// ** > * > + 的优先级
test3 := (a ** b) * c         // 外层括号可能被去掉
test4 := a ** (b * c)         // 内层括号必须保留
test5 := (a + b) ** c         // 括号必须保留

// 测试3: 混合位运算和算术运算
// ** > * > + > & > ^ > | > && > ||
test6 := ((a + b) & c) | d           // 检查多层括号
test7 := a ** b * c + d & e ^ f | g  // 无括号，测试自然优先级
test8 := (a | b) && (c & d)          // 括号必须保留（&和&&不同）

// 测试4: 复杂表达式
test9 := (((a)))                     // 多余的括号应该被去掉
test10 := ((a + b) * (c + d))       // 适当保留必要的括号
