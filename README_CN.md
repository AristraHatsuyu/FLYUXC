# FLYUX 编译器 (FLYUXC)

<div align="center">

![Version](https://img.shields.io/badge/version-0.1-blue.svg)
![C Standard](https://img.shields.io/badge/C-C11-green.svg)
![CMake](https://img.shields.io/badge/CMake-3.10+-orange.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

**一个支持 Unicode/Emoji 变量名的现代编程语言编译器**

[快速开始](#快速开始) • [文档](#文档) • [特性](#特性) • [架构](#架构) • [贡献](#贡献)

🇨🇳 **中文** | [🇺🇸 English](README.md)

</div>

---

## 🌟 亮点特性

### 🎨 Unicode/Emoji 变量名

FLYUX

```flyux
// 用 Emoji 与Unicode编程！
🚀 := "rocket";
🎯 := 100;

🤪🫵:<num>= (🐙, 🍄) {
    R> 🐙 + 🍄 * 🐙;
};

main := () {
    result := 🤪🫵(5, 3);  // 20
    print(result, 🚀, 🎯);
};
```

### ⚡ 高性能编译器

- **编译速度**: ~600ms (首次), ~500ms (后续)
- **二进制大小**: 76MB (静态链接 LLVM)
- **零外部依赖**: 仅依赖系统库，完全可移植

### 🔍 精确错误定位

```
ERROR: Type mismatch at line 15, column 10
        x:[num]= "string"
                 ^^^^^^^^
       Expected: num
       Got:      str
```

精确到**字节级别**的错误位置，支持多字节 UTF-8 字符。

### 🧠 智能类型推断

```flyux
x := 42;              // 自动推断为 num
name := "Alice";      // 自动推断为 str
active := true;       // 自动推断为 bl
```

## 🚀 快速开始

### 安装

```bash
# 1. 克隆项目
git clone https://github.com/AristraHatsuyu/flyuxc.git
cd flyuxc

# 2. 构建
cmake -B build
cmake --build build

# 3. 运行测试
./build/flyuxc testfx/valid/basic/demo.fx
```

### 第一个程序

创建 `hello.fx`:

```flyux
main := () {
    print("Hello, FLYUX! 🎉");
};
```

编译:

```bash
./build/flyuxc hello.fx
```

查看 **[QUICKSTART.md](QUICKSTART.md)** 了解更多示例。

## 📚 文档

### 📖 主要文档

| 文档 | 说明 |
|------|------|
| **[docs/INDEX.md](docs/INDEX.md)** | 📑 **完整文档索引** - 所有文档的导航入口 |
| **[docs/QUICKSTART.md](docs/QUICKSTART.md)** | 快速上手指南，5分钟入门 |
| **[docs/FLYUX_SYNTAX.md](docs/FLYUX_SYNTAX.md)** | 完整语法规范 |
| **[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** | 编译器架构设计详解 |

> 💡 更多文档（词法分析、语法分析、类型系统等）请查看 **[docs/INDEX.md](docs/INDEX.md)**

## ✨ 特性

### 核心功能 ✅

- [x] **完整的编译流程**
  - [x] 词法分析 (Lexer) - Unicode/Emoji 支持
  - [x] 语法分析 (Parser) - 完整 AST 构建
  - [x] 代码生成 (CodeGen) - LLVM IR 生成
  - [x] 二进制生成 - 原生可执行文件

- [x] **语言特性**
  - [x] 变量声明和赋值 (`x := 10`, `x = 20`)
  - [x] 函数定义和调用 (`func:<num>=(a,b) { R> a+b }`)
  - [x] 数组操作 (`[1,2,3]`, `arr[0]`, `arr.>len`)
  - [x] 对象操作 (`{key: value}`, `obj.key`)
  - [x] 控制流 (`if`, `L>` 循环)
  - [x] 运算符 (算术、比较、逻辑、位运算)
  - [x] 方法链 (`.>` 操作符)

- [x] **内置函数**
  - [x] `print()` / `println()` - 输出函数
  - [x] `len()` - 长度函数（统一的字符串/数组/对象长度）
  - [x] 类型检查函数

- [x] **优化特性**
  - [x] LLVM 静态链接 - 零 Homebrew 依赖
  - [x] 运行时内嵌 - 单文件分发
  - [x] 快速启动 - 仅初始化本地目标
  - [x] 完全可移植 - 可在任何 macOS 系统运行

### 技术亮点 🌟

- **静态链接**: 单一可执行文件，包含 LLVM
- **零依赖**: 仅依赖系统库 (libz, libc++, libSystem, CoreFoundation)
- **跨平台**: macOS (ARM64) 原生支持
- **现代化**: 支持 Unicode/Emoji 变量名和标识符

## 🏗️ 架构

FLYUX 编译器采用经典的多阶段编译器架构，基于 LLVM：

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 规范化    │ →  │  变量映射 │ →  │  词法分析  │ →  │ 语法分析  │
│ Normalize│    │  VarMap  │    │  Lexer   │    │  Parser  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                       ↓
┌──────────┐    ┌──────────┐    ┌──────────┐
│ 二进制    │ ←  │  LLVM    │ ←  │ 代码生成  │
│ Binary   │    │  Backend │    │ CodeGen  │
└──────────┘    └──────────┘    └──────────┘
```

### 技术栈

- **前端**: 纯 C11 实现的词法分析和语法分析
- **后端**: LLVM 20.1.6 (静态链接)
- **运行时**: 内嵌预编译运行时对象
- **构建**: CMake 3.13+

### 静态链接架构

```
flyuxc (76MB)
├── LLVM 静态库 (~70MB)
│   ├── Core, IRReader, Passes
│   └── AArch64 本地目标
├── 编译器代码 (~5MB)
└── 运行时对象 (~1MB, 内嵌)

动态依赖 (系统):
├── /usr/lib/libz.1.dylib
├── /usr/lib/libc++.1.dylib
├── /usr/lib/libSystem.B.dylib
└── CoreFoundation.framework
```

## 🔧 开发

### 系统要求

- **操作系统**: macOS (ARM64/x86_64) / Linux / Windows (WSL)
- **依赖**: 无需任何外部依赖（macOS 预编译二进制）
- **构建需求** (仅开发者):
  - CMake 3.13+
  - C11 编译器 (Clang/GCC)
  - C++17 编译器
  - LLVM 20.x (Homebrew)

### 构建

```bash
# 配置
cmake -B build

# 编译
cmake --build build

# 清理
rm -rf build
```

### VS Code 任务

项目配置了便捷的 VS Code 任务：

- **Cmd+Shift+B**: 构建项目
- **Tasks: Run Task** → "run": 运行测试

### 测试

```bash
# 运行测试用例
./build/flyuxc testfx/valid/basic/demo.fx
```

## 📖 语法示例

### 变量和类型

```flyux
// 隐式类型
name := "Alice";
age := 25;
active := true;

// 显式类型
score:[num]= 95;
username:[str]= "Bob";
```

### 函数

```flyux
// 类型标注函数
add:<num>=(x, y) {
    R> x + y;  // R> = return
};

// 调用
result := add(10, 20);

// 方法链调用
[1, 2, 3].>len.>println;  // 输出: 3
"Hello".>println;          // 输出: Hello
```

### 数组和对象

```flyux
// 数组
fruits := ["apple", "banana", "orange"];

// 对象
person := {
    name: "Charlie",
    age: 30,
    hobbies: ["reading", "coding"]
};

// 访问
print(person.name);
print(fruits[0]);
```

### 控制流

```flyux
// If 语句
if (x > 10) {
    print("大于10");
} {
    print("小于等于10");
};

// 循环
L> (i := 0; i < 10; i++) {  // L> = loop
    print(i);
};
```

查看 **[FLYUX_SYNTAX.md](FLYUX_SYNTAX.md)** 了解完整语法。

## 🤝 贡献

欢迎贡献代码、报告 Bug 或提出建议！

### 贡献流程

1. Fork 项目
2. 创建特性分支 (`git checkout -b feature/amazing`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing`)
5. 创建 Pull Request

### 开发指南

- 遵循 C11/C++17 标准
- 代码风格：4 空格缩进，清晰命名
- 文档：更新相关 `.md` 文件
- 测试：添加测试用例到 `testfx/valid/`

查看 **[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** 了解项目架构。

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 致谢

FLYUX 编译器的设计受到以下项目启发：

- **Rust Compiler** (rustc) - 模块化架构
- **LLVM** - 编译器基础设施
- **Go** - 简洁语法设计
- **TypeScript** - 类型推断系统

## 联系

- **文档**: 项目根目录的 `*.md` 文件
- **Bug 报告**: [GitHub Issues](https://github.com/AristraHatsuyu/FLYUXC/issues)
- **讨论**: [GitHub Discussions](https://github.com/AristraHatsuyu/FLYUXC/discussions)

---

<div align="center">

[⬆ 回到顶部](#flyux-编译器-flyuxc)

</div>
